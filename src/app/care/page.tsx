import { PagePlaceholder } from '@/components/page-placeholder/page-placeholder';

export default function CarePage() {
  return (
    <PagePlaceholder
      eyebrow="MVP feature two"
      title="Care"
      description="Watering, fertiliser and other care records will be kept together here."
      nextStep="Care tracking will begin once Plant Management is working, so every event can be tied to the correct plant."
    />
  );
}
