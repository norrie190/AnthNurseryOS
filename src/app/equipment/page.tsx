import { PagePlaceholder } from '@/components/page-placeholder/page-placeholder';

export default function EquipmentPage() {
  return (
    <PagePlaceholder
      eyebrow="MVP feature three"
      title="Equipment"
      description="A record of the equipment used to run the nursery and what has been invested in it."
      nextStep="Equipment tracking will be added as its own small feature after the plant and care stages."
    />
  );
}
