import { PagePlaceholder } from '@/components/page-placeholder/page-placeholder';

export default function PlantsPage() {
  return (
    <PagePlaceholder
      eyebrow="MVP feature one"
      title="Plants"
      description="The home for individual plants, their identity, status and nursery history."
      nextStep="Plant Management will be designed and built here after we agree the first database model."
    />
  );
}
