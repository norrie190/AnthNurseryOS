// Domain models are introduced only when their MVP phase begins.
// This establishes the PostgreSQL and Prisma migration boundary without prematurely
// committing to a plant, care, breeding, or financial data model.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
